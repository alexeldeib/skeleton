{
  "name": "cloudflare-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "echo 'No development server for cloudflare-worker (requires deployment)'",
    "build": "wrangler build",
    "deploy": "wrangler deploy",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230518.0",
    "typescript": "^5.1.6",
    "wrangler": "^3.6.0"
  }
}